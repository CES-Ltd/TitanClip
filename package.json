{
  "name": "titanclip",
  "version": "1.0.0",
  "description": "TitanClip - AI Company Orchestration Platform",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.electron.json && electron .",
    "build": "pnpm -r build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "start": "electron .",
    "dist": "pnpm run build && tsc -p tsconfig.electron.json && electron-builder --mac",
    "dist:win": "pnpm run build && tsc -p tsconfig.electron.json && electron-builder --win",
    "dist:linux": "pnpm run build && tsc -p tsconfig.electron.json && electron-builder --linux",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^33.0.0",
    "electron-builder": "^26.8.1",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "patchedDependencies": {
      "embedded-postgres@18.1.0-beta.16": "patches/embedded-postgres@18.1.0-beta.16.patch"
    }
  }
}
